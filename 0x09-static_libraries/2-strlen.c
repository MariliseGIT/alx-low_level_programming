#include <stdio.h>
#include "main.h"
/**
 * _strlen - Returns the length of a sting.
 * @s: char
 * Return: int
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	return (i);
}
